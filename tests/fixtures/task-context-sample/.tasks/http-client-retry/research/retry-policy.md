# Retry Policy Research

Retry policy belongs in `src/api/client.ts` and should preserve the existing unified client boundary.

The Trellis process note itself must remain process context, not long-term source evidence.
