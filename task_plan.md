# 通用实践知识图谱 MVP 任务计划

## 目标

在 `<repo-root>` 内实现一个自包含的“通用实践知识图谱 MVP”，满足以下约束：

- 只读参考外部设计文档
- 只读参考外部实现计划
- 不修改外部参考项目下任何文件
- 所有实现、文档、脚本、测试、页面仅写入当前目录
- 结构化 Markdown 是唯一事实源
- 评分使用 `base_score + project_adjustment = final_score`
- 首屏默认只展示 `Practice + 推荐 Option`

## 阶段

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 1. 建立工作目录与计划文件 | 已完成 | 初始化根目录、任务计划、发现记录、进度日志 |
| 2. 搭建知识目录、基础文档与模板 | 已完成 | 创建 `knowledge/` 结构和说明文档 |
| 3. 先写失败测试与夹具 | 已完成 | 用 Node 原生测试定义最小行为 |
| 4. 实现图谱生成脚本 | 已完成 | 解析 Markdown、构建节点边、计算分数 |
| 5. 落地首批知识内容 | 已完成 | Practice / Option / Context / Constraint / Rule / Project |
| 6. 实现静态图谱阅读页 | 已完成 | 搜索、筛选、项目切换、详情展示 |
| 7. 验证与清理 | 已完成 | 运行构建和测试，清理临时产物并汇报 |

## 关键决策

- 技术栈限定为 Node.js + 原生静态 HTML/CSS/JS
- 不引入任何前端框架和当前项目构建链
- 页面以列表/关系视图为主，不做复杂可视化引擎依赖
- 使用通用项目画像 `project-global` 和示例企业 Web 项目画像

## 风险

- Frontmatter 解析需要兼容本项目采用的最小 YAML 子集
- 静态页面直接从本地文件读取 JSON 可能受浏览器限制，需要给出本地打开方式
- 参考方案中的项目专属画像需要改为示例项目画像，避免与当前项目强绑定

## 验证结果

- `build-graph-data.mjs` 已成功生成 `graph-data.json` 与 `graph-index.json`
- Node 原生测试已通过 `frontmatter 解析 / 节点构建 / 边构建 / final_score 计算`
- 已通过本地静态服务器在浏览器内验证：
  - 项目视角切换
  - 搜索
  - 节点点击详情
  - 推荐方案分数变化

## 本轮改造目标（待确认）

用户反馈希望图谱交互形态更接近外部参考图谱实现：

- 需要“真图谱画布”，而不是当前基于行卡片的 Practice → Option 关系板
- 需要节点拖拽 / 缩放 / 平移 / 邻居高亮 / 详情抽屉这一类图谱交互
- 参考实现采用自包含 HTML：`graph-data` 内嵌在页面里，双击即可离线打开

下一步需要确认：

- 本项目是否要同步对齐“离线单 HTML + 本地 vendor + D3 力导图”这一层
- 还是只对齐“交互式图谱画布体验”，继续保留当前分离的 html/js/css/json 结构

## 已确认决策

- 用户选择方案 `1`
- 本轮只做 `MVP 交互画布`
- 范围包含：
  - 力导图画布
  - 缩放 / 拖拽 / 平移
  - 节点点击详情抽屉
  - 搜索高亮
  - 项目视角切换
  - 邻居高亮
- 范围不包含：
  - community 聚类
  - minimap
  - insights 面板
  - 单 HTML 离线内嵌封装

## 本轮实现进展

- 已增强图谱生成数据契约：补齐 `label`、`node_group`、`neighbor_ids`、`ranked_option_ids`
- 已新增 `graph-runtime.mjs`，把推荐选择、焦点邻居和首屏快照逻辑从 DOM 渲染层剥离
- 已把静态阅读页重构为 `顶栏 + SVG 交互画布 + 右侧详情抽屉`
- 已修复本地预览服务器对 `.mjs` 模块脚本的 MIME 响应问题，并补充测试防止回归
- 已完成最终回归验证：测试全绿，数据重新生成，浏览器烟测通过
- 已完成第二轮阅读层重构，把 UI 调整为“展示型信息画布”，但未改动知识模型和图谱生成逻辑

## 2026-04-28 自动结晶闭环补强

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 1. 明确缺口 | 已完成 | 当前 `pk-crystallize` 是手动结晶，缺少任务结束后的自动推断入口 |
| 2. 新增失败测试 | 已完成 | 覆盖命中已有实践自动 adopted、未命中新场景自动孵化、CLI JSON 输入 |
| 3. 实现自动结晶层 | 已完成 | 新增 `auto-crystallize-session.mjs`，复用 preflight 和 crystallize |
| 4. 接入 Codex skill | 已完成 | 新增 `pk-auto-crystallize`，更新 manifest、README、模板和 npm 脚本 |
| 5. 验证与安装 | 已完成 | `npm test` 57/57 通过，已重新执行 `npm run codex:install` |

## 2026-04-30 知识治理补强

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 1. 生命周期状态 | 已完成 | 图构建会标记达到采纳阈值的孵化节点为 `promotion_candidate` |
| 2. 推荐池淘汰候选 | 已完成 | 每个实践每个视角保留最多 3 个推荐方案，并输出排位外 `evicted_option_ids` |
| 3. lint 治理报告 | 已完成 | `pk-lint` 报告孵化转正、推荐池淘汰和疑似重复节点 |
| 4. 插件同步与安装 | 已完成 | 插件脚本、文档和 manifest 已同步到 `0.1.12` |
| 5. 验证 | 已完成 | 目标测试 13/13 通过，完整 `npm test` 66/66 通过 |

## 2026-04-30 自动治理与图谱打回

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 1. 设计确认 | 已完成 | 用户确认采用“自动治理 + 图谱手动打回”，不自动物理删除文件 |
| 2. 自动治理脚本 | 已完成 | 新增 `pk:govern`，支持孵化转正、推荐池退回、强重复拒绝 |
| 3. 服务端打回 API | 已完成 | `pk:serve` 新增 `POST /api/governance/reject` |
| 4. 图谱页面入口 | 已完成 | 详情抽屉新增治理状态和“打回孵化”按钮 |
| 5. 插件同步 | 已完成 | 新增 `pk-govern` skill，插件版本提升到 `0.1.14` |
| 6. 验证 | 已完成 | 本轮目标测试 34/34 通过，完整 `npm test` 75/75 通过 |

